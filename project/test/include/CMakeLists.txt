# Configure a header file to pass the version number to the source code
# From the "TutorialConfig.h.in" input file, CMake generates the header 
# "TutorialConfig.h"
configure_file(TutorialConfig.h.in TutorialConfig.h)
